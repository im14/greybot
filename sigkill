SIGKILL is exactly the same as SIGTERM with that difference that it doesn't invoke a process' signal handlers. You should *never* use it unless you *know* the process is bugging out in its signal handler.
