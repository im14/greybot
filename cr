Carriage Return (ASCII 13). Often depicted ^M or \r. Carriage returns are found just before newlines in text files generated by MS-DOS/Windows applications. They are poisonous. Remove them with tr -d '\r' < myfile > newfile # or # ed -s file <<< $'g/.*/s/\r$//\nw'
