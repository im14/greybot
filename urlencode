# urlencode() { while read -n 1 c; do [[ $c = [0-9a-zA-Z-_.~] ]] && printf %s "$c" || printf %%%x "'$c"; done <<< "$1"; }; urlencode "The Bars - Foo.mp3" # or see http://bash.projectnet.org/wiki/index.php/Urlencode
