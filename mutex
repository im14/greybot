A mutual exclusion lock ensures your script can only be running once. mkdir "$lockdir" || exit 1; ...; rm -rf "$lockdir" # Another option: exec 9>> "$lockfile"; { [[ $(fuser -f "$lockfile") == $$ ]] || exit 1; } 2>&- 9>&- # (fuser also works read-only and cleanup is implicit) See http://mywiki.wooledge.org/BashFAQ/045 http://wiki.bash-hackers.org/howto/mutex
