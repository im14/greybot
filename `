The backquote (`) is used in the old-style command substitution, e.g. foo=`command`. The foo=$(command) variety is recommended over this syntax. Backslash handling inside $() is less surprising, and $() is easier to nest. See http://wooledge.org/mywiki/BashFAQ#faq82
