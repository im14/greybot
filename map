Bash does not have "maps" or "hashes" or "associative arrays". Bash can only map *integers* to *strings* using Arrays. If you need a map, rework your program to use arrays instead or migrate to awk/perl/...
